import { VersionUpgrade } from '@uniswap/token-lists';

/**
 * Returns the higher of two version upgrade types
 * @param version1
 * @param version2
 */
export declare function maxVersionUpgrade(version1: VersionUpgrade, version2: VersionUpgrade): VersionUpgrade;
